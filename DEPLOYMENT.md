# دليل نشر موقع "حارس ونقاء" على استضافة Hostinger (PHP / MySQL)

تم تصميم وتطوير هذا الموقع ليعمل بكفاءة عالية على **استضافة Hostinger المشتركة (Shared Web Hosting)** التي تدعم **PHP 8.x و MySQL / MariaDB** دون الحاجة لتشغيل خادم Node.js على الاستضافة.

---

## المكونات المضمنة في المشروع

1. **واجهة React الحديثة (SPA):** مبنية بأحدث تقنيات React 19 + TypeScript + Tailwind v4 ويتم تجميعها في مجلد `dist`.
2. **واجهة برمجة التطبيقات (REST API):** مجلد كامل `public/api/` مكتوب بـ Pure PHP 8.x عبر PDO لحماية ضد ثغرات الحقن (SQL Injection).
3. **مخطط قاعدة البيانات والبيانات الأولية:** ملف `database.sql` و `public/database.sql` جاهز للاستيراد بترميز `utf8mb4_unicode_ci`.
4. **التوجيه والحماية:** ملف `.htaccess` يدعم مسارات React Router وتمرير طلبات `/api/*` مباشرة للباك إند مع حماية ملفات التكوين.

---

## خطوات النشر خطوة بخطوة على Hostinger hPanel

### الخطوة 1: إنشاء قاعدة بيانات MySQL
1. سجل الدخول إلى لوحة تحكم **Hostinger hPanel**.
2. من القائمة الجانبية، توجه إلى **Databases** (قواعد البيانات) -> **Management** (الإدارة).
3. اضغط على **Create New MySQL Database And Database User**:
   - **Database Name:** أدخل اسماً لقاعدة البيانات (مثال: `u123456789_hares`).
   - **Username:** أدخل اسم المستخدم لقاعدة البيانات (مثال: `u123456789_user`).
   - **Password:** أنشئ كلمة مرور قوية واحتفظ بها.
4. اضغط **Create**.

---

### الخطوة 2: استيراد ملف `database.sql` عبر phpMyAdmin
1. في نفس صفحة قواعد البيانات بـ hPanel، اضغط على زر **Enter phpMyAdmin** بجوار قاعدة البيانات التي أنشأتها.
2. من القائمة العلوية داخل phpMyAdmin، اختر تبويب **Import** (استيراد).
3. اضغط على **Choose File** (اختيار ملف) وحدد ملف `database.sql` من مجلد المشروع.
4. تأكد من أن الترميز محدد على **utf-8** ثم انزل لأسفل واضغط **Import**.
5. ستظهر رسالة خضراء تفيد بنجاح استيراد 15 جدولاً مع بيانات الموقع الأولية باللغتين العربية والإنجليزية.

---

### الخطوة 3: تعديل بيانات الاتصال في `api/config.php`
افتح الملف `api/config.php` (الموجود داخل مجلد `public/api/` أو بعد الرفع في `public_html/api/`)، وضع بيانات قاعدة البيانات:

```php
// ==============================================================================
// 1. Database Configuration
// ==============================================================================
define('DB_HOST', 'localhost');                  // عادة ما يكون localhost في Hostinger
define('DB_NAME', 'u123456789_hares');           // اسم قاعدة البيانات التي أنشأتها
define('DB_USER', 'u123456789_user');            // اسم مستخدم قاعدة البيانات
define('DB_PASS', 'YOUR_STRONG_DB_PASSWORD');     // كلمة المرور التي اخترتها
define('DB_CHARSET', 'utf8mb4');

// مفتاح Gemini AI الاختياري (يمكن تركه فارغاً أو تعيينه للاستفادة من مزايا الذكاء الاصطناعي):
define('GEMINI_API_KEY', 'AIzaSy...');
```

---

### الخطوة 4: بناء وتصدير ملفات الواجهة الأمامية
على جهازك المحلي، قم بتنفيذ الأمر التالي:

```bash
npm run build
```

سيقوم الأمر ببناء وتجهيز جميع الملفات المحسنة داخل مجلد `dist/` مع نقل مجلد `api` وملف `.htaccess` و `database.sql` تلقائياً.

---

### الخطوة 5: رفع الملفات إلى `public_html`
1. في لوحة تحكم **Hostinger hPanel**، افتح **File Manager** (مدير الملفات) للموقع.
2. ادخل إلى المجلد الرئيسي للموقع: `public_html/`.
3. قم برفع **محتويات مجلد `dist/`** كاملة (وليس المجلد نفسه، بل الملفات التي بداخله):
   - `index.html`
   - `.htaccess`
   - مجلد `assets/`
   - مجلد `api/` (يحتوي على جميع ملفات الباك إند PHP)
   - ملف `database.sql` (اختياري للاحتفاظ بنسخة)
   - أي أيقونات أو صور في الجذر.

> **ملاحظة للملفات المخفية:** تأكد من رفع ملف `.htaccess` لأنه مسؤول عن تشغيل مسارات React Router وتوجيه طلبات API بشكل صحيح.

---

### الخطوة 6: التأكد من الصلاحيات والإعدادات
- تأكد أن مجلد `public_html` ومجلد `public_html/api` يملكان تصريح `755`.
- تأكد أن ملفات PHP تملك تصريح `644`.
- تأكد في Hostinger من اختيار إصدار **PHP 8.1 أو 8.2 أو 8.3** (من قسم **Advanced** -> **PHP Configuration**).

---

## بيانات تسجيل الدخول الافتراضية للوحة التحكم
- **رابط اللوحة:** `https://your-domain.com/admin`
- **اسم المستخدم الافتراضي:** `admin`
- **كلمة المرور الافتراضية:** `admin123`

> **أمان فوري:** عند أول تسجيل دخول بكلمة المرور الافتراضية، يقوم النظام آلياً بتشفير كلمة المرور وتحديثها في قاعدة البيانات عبر خوارزمية **Bcrypt** القوية. يمكنك تغيير كلمة المرور من داخل لوحة التحكم أو عبر واجهة phpMyAdmin.

---

## اختبار صحة الـ API
يمكنك التحقق من اتصال قاعدة البيانات بالدخول عبر المتصفح إلى:
```
https://your-domain.com/api/content.php
```
إذا كان التكوين صحيحاً، ستظهر لك استجابة JSON تحتوي على `{"success":true,"settings":{...}}`.

---

تم بحمد الله إعداد الموقع ليكون جاهزاً للعمل بنسبة 100% وبأعلى كفاءة وأمان على استضافة Hostinger.
